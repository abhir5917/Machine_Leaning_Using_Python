# string, list, tuple.dictionary
# set, frozenset

#mutable - list & disctionary - when you can do the modification to the exsting itesms
# immutable - string
var = 'dhoni'
print(var)
print(type(var))
